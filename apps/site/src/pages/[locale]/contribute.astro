---
import { getNonDefaultLocales } from '../../lib/i18n';
import Page from '../contribute.astro';

export function getStaticPaths() {
  return getNonDefaultLocales().map((locale) => ({ params: { locale } }));
}
---

<Page />
